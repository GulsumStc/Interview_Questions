package J00_Interview_Questions;

public class i31_throw_throws_keyword {

/*
	 What is the difference between "throw" and "throws" keywords?

	 1)"throw" is written inside the method body, "throws" is written between method parentheses
	   and the opening curly brace
	 2)After "throw" keyword we type ==> new + constructor of exception class
	   After "throws" keyword we type ==> name of the exception class
	 3)We can throw multiple exceptions by using just one "throws" keyword
	   We cannot throw multiple exceptions by using just one "throw" keyword.
	   If you want to throw more than 1 exceptions by using "throw" keyword you have to type
	   "throw" for each exception.
	 4)"throws" is written only once.
	   "throw" is written wherever we want inside the method and as much as we want
	*/

        /*
        1)"thorw" ile "thorws" keyword'leri arasındaki fark nedir?
          "throw" method body'si içinde, "throws" method body parantezinden hemen sonra kullanılır
        2)"throw" method body'si içinde istenilen yerde istenildiği kadar kullanılır,
          "throws" ise method parantezinden hemen sonra bir kere kullanılabilir
        3)"throw"dan sonra "new" keyword ve constructor kullanılarak object oluşturulur
          "throws"dan sonra sadece Exceptions Class ismi yazılır
        4)"throw" belli şartlar için Exception atmada kullanılır,
          "throws" ise application'ı Exception attıktan sonra durdurmak için kullanılır.
        */




}
